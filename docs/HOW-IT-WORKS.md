# How this works

Findings that are not obvious from the code, in the order they matter.

## The Arena's game-side protocol

The Arena injects `dinput8.dll` into the game (it writes it into the game folder
at launch and deletes it afterwards; it is stored as a managed resource inside
`BfmeFoundationProject_OnlineArena.exe`). The DLL hooks D3D9 `Present`, draws the
overlay, and serves a named pipe.

- The Arena writes `arena_api_interface.conf` into the game folder containing a
  GUID; the add-on serves `\\.\pipe\bfme_api_interface_<guid>`.
- The Arena connects as a **client, once per request**, with a 3 s connect
  timeout and a 30 s read timeout, serialised behind a single lock.

Commands seen on the wire (`<command>\n<space separated args>`):

| Command | Reply |
|---|---|
| `getViewportSize` | `2560 1440`, or `0 0` before the first frame |
| `inputMove x y` | `OK` — moves the in-game pointer |
| `inputClick x y` | `OK` |
| `inputSetLock 1` | `OK` — `ClipCursor` around the game |
| `getPixelColor x y` | `R G B` |
| `updateOverlayMarkup ...` | `OK` — the `SYSTEM TEST` overlay |
| `getNetworkStats` | `peersKnown=…;fps=…;…` |
| errors | anything starting with `ERR_` (`ERR_NOT_ARMED`, `ERR_BAD_COMMAND`, `ERR_COMMAND_UNKNOWN`) |

`ERR_NO_CONNECTION` is generated by the Arena itself when the pipe call fails.

### Why the resolution cannot change

The coordinates are compile-time constants for a 2560x1440 game. Reporting a
different viewport rescales only the *overlay*: the input and pixel coordinates
stay at `700,1357`, `1070,1357`, `2415,1357`. There is no runtime scaling.

### How the Arena decides a button is ready

It hovers and waits for the highlight, then clicks:

```
inputMove 700 1357   -> OK
getPixelColor 700 1357 -> 0 0 0 … 41 13 6   (the button fading in)
getPixelColor 700 1357 -> 244 234 219       (hover highlight — now it clicks)
inputClick 700 1357
```

If the pointer never actually arrives, the pixel stops at `41 13 6` and the whole
launch times out with `TaskCanceledException: A task was canceled` out of
`BfmeClient.LaunchGame` → `WaitWhile`. That message means "a 30 s poll expired",
nothing more specific — it is the same error for a missing overlay, a dead pipe
and a mis-aimed pointer.

## Why the window position was the whole problem

`WineWindow.constrainFrameRect:toScreen:` only skips Cocoa's constraint when the
window **completely covers a screen**. Any other window gets pushed below the
menu bar, which moves the Win32 origin down by the menu bar height (33 points, so
66 Win32 pixels under a 2x Retina scale).

That single fact explains every configuration that failed:

| Wine desktop | Display | Game window at | Arena test |
|---|---|---|---|
| 2560x1440, no Retina | 1728x1117 | `0,0` (bigger than the screen, so unconstrained) | **passes** |
| 2560x1440, Retina 2x | 1728x1117 | `0,66` | fails |
| 2560x1440, no Retina | 2560x1600 | `0,30` | fails |
| 1728x1080, no Retina | 1728x1117 | `0,33` | fails |
| 2560x1440, Retina 1.48, `ConstrainWindows=N` | 1728x1117 | `0,0` | **passes** |

Pinning the window back to `0,0` with `SetWindowPos` is not enough — Cocoa keeps
its own idea of where the frame is, and the pointer still lands offset. It has to
be fixed in the driver.

Confirmation, hovering one button while sweeping the y coordinate with the window
at `0,66`:

```
hover 700,1357 -> 41 13 6        (nothing)
hover 700,1423 -> 230 179 106    (highlight — exactly +66)
```

## Display geometry

The virtual desktop is fixed at 2560x1440. `bfme-config.sh` picks

```
scale = max(2560 / screen_width_points, 1440 / screen_height_points, 1)
```

so the window is as large as it can be while still fitting. On a 1728x1117
screen that is 1.481481 → 1728x972 points: full width, a 145-point letterbox at
the bottom from 16:9 in a 16:10.4 screen. On a 16:9 display it fills exactly.

## Performance

- Rosetta's x87 emulation is the wall — roughly 35x slower than its integer and
  SSE2 paths. x87sidecar is what makes the game playable; nothing in Wine's own
  configuration comes close.
- Frame rate is capped by the engine at ~38 fps, and that cap is reached at
  UltraHigh/2560x1440, so there is no reason to lower settings.
- `StaticGameLOD = Low` is not a valid value for this engine and makes it re-run
  its CPU benchmark on every boot (103 s to first frame instead of 18 s). Use
  `UltraHigh`, or the override in `config/gamelod-fastload.ini` on a slower
  machine.
- Do not kill `wineserver` between runs; a cold start costs about 90 s.

## Measurement traps

Every one of these produced a wrong number at some point:

- Screenshotting a screen *region* rather than the game window by `CGWindowID`
  captures the desktop, the terminal, or a stale window.
- The game window looks black to per-window capture until wined3d has actually
  presented frames; wait for `first frame presented` in `arenaapilog.txt`.
- Perl's `time()` is integer seconds. Use `-MTime::HiRes=time` or a 9.58 s load
  reads as 10 s.
- `make dlls/winemac.drv/winemac.drv.so` silently builds nothing. The real target
  is `dlls/winemac.drv/winemac.so`.
- Leaked `wintool pin` / `winwatch` polling loops accumulate across runs and slow
  the whole machine down. They take a duration argument; use it.
